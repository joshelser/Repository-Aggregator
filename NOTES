Perl Modules Used:
     Git::Wrapper <-- Hack upon
     Date::Parse
     DBI
     Config::Abstract::Ini
     POSIX qw(strftime)

ToDo:
		Refactor framework/lib/scripts.php (ability to add JS from the top php level w/o having to dive into the framework)
